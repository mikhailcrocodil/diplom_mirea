import { UserRole } from "../../../../../../api/models";

export const useDropdownItems = () => {
  const getItems = (role: UserRole) => {
    switch (role) {
      case UserRole.ADMIN: {
      }
    }
  };
};
